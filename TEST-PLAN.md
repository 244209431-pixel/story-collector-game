# 数据安全保障系统 - 测试计划

**版本：** v11.2  
**开发时间：** 2026-05-06  
**测试负责人：** AI Agent

---

## 一、测试范围

| 模块 | 功能点 | 测试类型 |
|------|--------|----------|
| IndexedDB 持久备份 | 自动备份、恢复、清理 | 单元测试 + 集成测试 |
| 增强型云端同步 | 数据完整性验证、双向合并 | 单元测试 + 集成测试 |
| 数据补录工具 | 日历视图、补录对话框、批量操作 | 端到端测试 |
| 启动时恢复引导 | 自动检测备份、用户选择恢复 | 端到端测试 |
| 导出/导入增强 | 预览、验证、加密 | 单元测试 + 端到端测试 |

---

## 二、测试用例

### 模块 1：IndexedDB 持久备份

#### 测试用例 1.1：自动备份
- **操作：** 执行 `save()` 函数
- **预期：** IndexedDB 中新增一条备份记录
- **验证方法：** 检查 IndexedDB 数据

#### 测试用例 1.2：启动时恢复（有备份）
- **操作：** 清空 localStorage，保留 IndexedDB
- **预期：** 自动从 IndexedDB 恢复数据
- **验证方法：** 检查 G 对象数据

#### 测试用例 1.3：启动时恢复（无备份）
- **操作：** 清空 localStorage 和 IndexedDB
- **预期：** 使用默认数据
- **验证方法：** 检查 G 对象数据

#### 测试用例 1.4：清理旧备份
- **操作：** 添加 10 条备份记录
- **预期：** 只保留最近 7 天
- **验证方法：** 检查 IndexedDB 记录数

---

### 模块 2：增强型云端同步

#### 测试用例 2.1：云端数据完整性验证（通过）
- **操作：** 云端数据包含 `history`（至少 1 条）
- **预期：** 允许覆盖本地
- **验证方法：** 检查 `cloudLoad()` 返回值

#### 测试用例 2.2：云端数据完整性验证（失败）
- **操作：** 云端数据 `history = {}`, `medals = []`
- **预期：** 拒绝覆盖本地
- **验证方法：** 检查 `cloudLoad()` 返回值

#### 测试用例 2.3：双向合并（本地有数据，云端有数据）
- **操作：** 本地有 3 月数据，云端有 4 月数据
- **预期：** 合并后包含 3 月和 4 月数据
- **验证方法：** 检查合并后的 `history`

#### 测试用例 2.4：版本号保护
- **操作：** 云端数据版本 `v10`，本地版本 `v11`
- **预期：** 拒绝云端覆盖
- **验证方法：** 检查 `cloudLoad()` 逻辑

---

### 模块 3：数据补录工具

#### 测试用例 3.1：日历视图显示
- **操作：** 打开 `?mode=recovery` 页面
- **预期：** 显示日历，已打卡日期高亮
- **验证方法：** 视觉检查

#### 测试用例 3.2：单日补录
- **操作：** 点击空白日期 → 填写补录信息 → 保存
- **预期：** 该日期添加到 `history`，日历更新
- **验证方法：** 检查 `G.history`

#### 测试用例 3.3：批量补录
- **操作：** 选择日期范围 → 批量填充
- **预期：** 所有日期添加到 `history`
- **验证方法：** 检查 `G.history`

#### 测试用例 3.4：勋章补录
- **操作：** 补录时选择勋章
- **预期：** 勋章添加到 `G.medals`
- **验证方法：** 检查 `G.medals`

#### 测试用例 3.5：故事补录
- **操作：** 补录时填写故事
- **预期：** 故事添加到 `G.myStories`
- **验证方法：** 检查 `G.myStories`

---

### 模块 4：启动时恢复引导

#### 测试用例 4.1：检测到 IndexedDB 备份
- **操作：** 启动页面，IndexedDB 有备份
- **预期：** 显示恢复引导对话框
- **验证方法：** 视觉检查

#### 测试用例 4.2：用户选择恢复
- **操作：** 点击「恢复」按钮
- **预期：** 数据恢复，跳转到主页面
- **验证方法：** 检查 G 对象数据

#### 测试用例 4.3：用户选择跳过
- **操作：** 点击「跳过」按钮
- **预期：** 正常启动，不恢复
- **验证方法：** 视觉检查

---

### 模块 5：导出/导入增强

#### 测试用例 5.1：导出数据
- **操作：** 点击「导出数据」按钮
- **预期：** 下载 JSON 文件
- **验证方法：** 检查下载的文件

#### 测试用例 5.2：导入数据（正常）
- **操作：** 上传有效的 JSON 文件
- **预期：** 数据恢复，显示成功提示
- **验证方法：** 检查 G 对象数据

#### 测试用例 5.3：导入数据（用户不匹配）
- **操作：** 上传其他用户的数据文件
- **预期：** 显示错误提示
- **验证方法：** 检查提示信息

#### 测试用例 5.4：导入前预览
- **操作：** 选择文件后，显示预览
- **预期：** 显示「将恢复 X 天打卡记录、X 个勋章」
- **验证方法：** 视觉检查

---

## 三、端到端测试场景

### 场景 1：数据丢失后自动恢复
1. 清空 localStorage
2. 刷新页面
3. **预期：** 自动从 IndexedDB 恢复

### 场景 2：云端数据损坏
1. 手动修改云端数据（设为空）
2. 执行 `cloudLoad()`
3. **预期：** 拒绝覆盖，保留本地数据

### 场景 3：完整补录流程
1. 打开补录页面
2. 补录 5 天打卡记录
3. 补录 2 个勋章
4. 补录 1 个故事
5. 点击「完成」
6. **预期：** 数据保存到 localStorage，跳转到主页面

### 场景 4：多设备同步
1. 设备 A：添加数据 → 同步到云端
2. 设备 B：首次打开 → 从云端恢复
3. **预期：** 设备 B 数据与设备 A 一致

---

## 四、测试执行记录

| 测试用例 | 状态 | 执行时间 | 备注 |
|---------|------|----------|------|
| 1.1 | ⏳ 待测试 | | |
| 1.2 | ⏳ 待测试 | | |
| 1.3 | ⏳ 待测试 | | |
| 1.4 | ⏳ 待测试 | | |
| 2.1 | ⏳ 待测试 | | |
| 2.2 | ⏳ 待测试 | | |
| 2.3 | ⏳ 待测试 | | |
| 2.4 | ⏳ 待测试 | | |
| 3.1 | ⏳ 待测试 | | |
| 3.2 | ⏳ 待测试 | | |
| 3.3 | ⏳ 待测试 | | |
| 3.4 | ⏳ 待测试 | | |
| 3.5 | ⏳ 待测试 | | |
| 4.1 | ⏳ 待测试 | | |
| 4.2 | ⏳ 待测试 | | |
| 4.3 | ⏳ 待测试 | | |
| 5.1 | ⏳ 待测试 | | |
| 5.2 | ⏳ 待测试 | | |
| 5.3 | ⏳ 待测试 | | |
| 5.4 | ⏳ 待测试 | | |

---

## 五、测试完成标准

- [ ] 所有单元测试用例通过
- [ ] 所有集成测试用例通过
- [ ] 所有端到端测试场景通过
- [ ] 无控制台错误
- [ ] 无数据丢失风险
- [ ] 性能可接受（备份/恢复 < 1s）

---

**测试完成后，填写执行时间和结果，并提交报告。**
