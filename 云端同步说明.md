# ☁️ 云端同步功能说明

## ⚠️ 当前状态（v11.2）

**云端同步功能暂时不可用！**

原因：
1. **CORS 跨域限制**：jsonblob.com 不支持从 GitHub Pages 域名跨域请求
2. **404 错误**：可能 blobId 已过期或被删除

---

## ✅ 替代方案（推荐）

### 方案 1：IndexedDB 本地备份（自动）

**功能**：每次打卡时自动备份到浏览器的 IndexedDB 数据库

**优点**：
- ✅ 自动备份，无需手动操作
- ✅ 数据存储在本地，不会丢失
- ✅ 浏览器崩溃或清空缓存时可以恢复

**查看备份数据**：
1. 按 F12 打开开发者工具
2. 切换到 **Application（应用）** 标签页
3. 左侧找到 **IndexedDB** → **StoryGameDB**
4. 可以看到 ` backups` 数据库表

---

### 方案 2：手动导出/导入（推荐）

#### 导出数据（备份）
1. 进入"设置"页面
2. 点击 **"📤 导出数据"** 按钮
3. 浏览器会自动下载一个 JSON 文件（例如：`故事收集家-备份-2026-05-06.json`）
4. **建议每周导出一次**，保存到安全位置（如微信收藏、云盘等）

#### 导入数据（恢复）
1. 进入"设置"页面
2. 点击 **"📥 导入数据"** 按钮
3. 选择之前导出的 JSON 文件
4. 系统会自动合并数据（不会覆盖新数据）

---

### 方案 3：使用导出文件在多设备间同步

如果你想在手机和电脑间同步数据：

1. **在设备 A（数据源）**：
   - 导出数据 → 得到 `故事收集家-备份-xxx.json`
   - 通过微信/邮件等方式发送到设备 B

2. **在设备 B（数据目标）**：
   - 登录同一个用户（如"棠棠"）
   - 导入从设备 A 发送过来的 JSON 文件
   - 系统会自动合并数据

---

## 🔧 为什么不用云端同步了？

### 技术原因
1. **CORS 跨域限制**：jsonblob.com 的服务器不允许从 GitHub Pages 跨域请求
2. **免费服务不稳定**：jsonblob.com 是免费服务，可能随时停止服务或更改接口
3. **隐私考虑**：打卡数据存储在第三方服务器，存在隐私风险

### 替代方案的优势
1. **IndexedDB 本地备份**：数据在本地，更安全
2. **手动导出/导入**：用户完全掌控数据，可以自己选择备份频率和存储位置
3. **跨设备同步**：通过导出/导入文件，可以手动在多设备间同步

---

## 📋 最佳实践

### 每天
- ✅ 正常打卡（IndexedDB 会自动备份）

### 每周
- ✅ 导出一次数据，保存到安全位置

### 换设备时
1. 在旧设备导出数据
2. 在新设备导入数据
3. 继续打卡

### 浏览器清空缓存后
1. 重新登录
2. 导入之前导出的备份文件
3. 继续打卡

---

## 🆘 数据恢复指南

### 情况 1：浏览器崩溃，但 IndexedDB 还在
1. 重新打开游戏页面
2. 登录同一个用户
3. 系统会自动从 IndexedDB 恢复数据

### 情况 2：浏览器清空了缓存，IndexedDB 也丢失了
1. 找到之前导出的备份文件（JSON）
2. 登录同一个用户
3. 导入备份文件
4. 继续打卡

### 情况 3：没有任何备份
- ❌ 无法恢复，只能重新开始
- 💡 建议：每周导出一次备份，避免这种情况

---

## 📣 未来的计划

我们正在考虑以下方案来改进数据同步：

1. **自建后端服务**：使用腾讯云/阿里云等可靠服务
2. **WebRTC P2P 同步**：设备间直接同步，无需服务器
3. **二维码扫码同步**：手机扫码，将数据传输到电脑

如果你有更好的建议，请告诉我！

---

**当前版本**：v11.2  
**最后更新**：2026-05-06